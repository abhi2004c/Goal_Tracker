{
  "name": "focusflow",
  "version": "1.0.0",
  "type": "module",
  "description": "Project Management & Productivity App with AI Planning",
  "scripts": {
    "setup": "cd backend && npm install && npx prisma generate && npx prisma db push && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": ["project-management", "productivity", "ai", "react", "nodejs"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}